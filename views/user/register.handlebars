{{#each error}}
    <div class="alert alert-danger">{{err}}</div>
{{/each}}

<div class="container">
    <h4>Cadastro de usuário</h4>
    <div class="card p-3">
        <form action="/user/register" method="post">
            <div class="form-group">
                <label for="name">Nome</label>
                <input required="true" {{#if values}} value="{{values.name}}" {{/if}} type="text" class="form-control" id="name" name="name" aria-describedby="name">
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input required="true" {{#if values}} value="{{values.email}}" {{/if}} type="email" class="form-control" id="email" name="email" aria-describedby="email">
            </div>
            <div class="form-group">
                <label for="password">Senha</label>
                <input required="true" type="password" class="form-control" id="password" name="password">
                
                <label for="password2">Repita a senha</label>
                <input required="true" type="password" class="form-control" id="password2" name="password2">
            </div>
                <button type="submit" class="btn btn-dark">Criar conta</button>
        </form>
    </div>
</div>

